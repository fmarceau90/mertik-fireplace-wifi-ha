send_command:
  name: Send Command
  description: Sends a raw HEX command to the Mertik device.
  fields:
    command:
      name: Command
      description: The hex string to send (e.g., '3430303103').
      required: true
      selector:
        text:
